apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm
  labels:
    app: crm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crm
  template:
    metadata:
      labels:
        app: crm
    spec:
      containers:
       - name: crm
         image: prasanth202/ubuntu-image-demo:latest
         ports:
         - containerPort: 80

